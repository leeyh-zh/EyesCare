<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SwitchStyle" parent="@android:style/Widget.Button.Toggle">
        <item name="android:track">@drawable/switch_track_holo_light</item>
        <item name="android:thumb">@drawable/switch_thumb_holo_light</item>
        <item name="android:textOn">" "</item>
        <item name="android:textOff">" "</item>
        <item name="android:thumbTextPadding">10dip</item>
        <item name="android:switchMinWidth">43dip</item>
        <item name="android:switchPadding">0dip</item>
    </style>

    <style name="colorPopupWindow" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/pop_in</item>
        <item name="android:windowExitAnimation">@anim/pop_exit</item>
    </style>

    <style name="translucent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@style/AnimationActivity</item>

    </style>

    <style name="Widget.SeekBar.Normal" parent="@android:style/Widget.SeekBar">
        <item name="android:maxHeight">4.0px</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">
            @android:drawable/progress_indeterminate_horizontal
        </item>
        <item name="android:progressDrawable">@drawable/seekbar_horizontal</item>
        <item name="android:minHeight">4.0px</item>
        <item name="android:thumb">@mipmap/detail_icon_schedule_ball_on</item>
        <item name="android:thumbOffset">10px</item>
    </style>

    <style name="AnimationActivity" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/push_left_in</item>
        <item name="android:activityOpenExitAnimation">@anim/push_left_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/zoom_enter</item>
        <item name="android:activityCloseExitAnimation">@anim/zoom_exit</item>
    </style>

    <style name="DialogTransparent" parent="@android:style/Theme.Dialog">
        <!-- 背景颜色及透明程度  -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--  是否有标题  -->
        <item name="android:windowNoTitle">true</item>
        <!--  是否浮现在activity之上  -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否模糊  -->
        <item name="android:backgroundDimEnabled">true</item>
        <!--全屏-->
        <item name="android:windowFullscreen">true</item>
        <!--activity切换动画-->
        <!--<item name="android:windowAnimationStyle">@style/AnimationDialogActivity</item>-->
        <item name="android:backgroundDimAmount">0.4</item>
        <item name="android:windowCloseOnTouchOutside">false</item>
    </style>
</resources>
